# Copy to `secrets.toml` in this folder (secrets.toml is gitignored).
# Streamlit exposes these as st.secrets — the app reads GROQ_API_KEY for LLM calls.
# https://docs.streamlit.io/develop/concepts/connections/secrets-management

GROQ_API_KEY = ""
